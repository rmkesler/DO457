---
- name: Backup IOS XE configurations
  hosts: ios
  gather_facts: false
  tasks:
    - name: Backup IOS XE configurations
      cisco.ios.ios_config:
        backup: true
        backup_options:
          dir_path: /home/student/task-workflow
          filename: "{{ inventory_hostname }}.txt"
